# custom_bytetrack.yaml
# Tuned ByteTrack settings for more stable track IDs across occlusion/jitter.
tracker_type: bytetrack
track_high_thresh: 0.4
track_low_thresh: 0.1
new_track_thresh: 0.4
track_buffer: 60
match_thresh: 0.8
fuse_score: True
